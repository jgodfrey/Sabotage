extends Node

#warning-ignore-all:unused_class_variable

onready var screenSize = get_viewport().get_visible_rect().size
onready var screenMidX = screenSize.x * 0.5
onready var screenMidY = screenSize.y * 0.5
